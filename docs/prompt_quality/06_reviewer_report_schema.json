{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Reviewer Report",
  "description": "Versioned Reviewer report assembled in code from deterministic scores and binary LLM judgments.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "rubric_version",
    "overall_status",
    "rubric_scores",
    "judgment_reasons",
    "issues",
    "requires_refinement",
    "refinement_instruction",
    "not_applicable"
  ],
  "properties": {
    "rubric_version": {
      "type": "string",
      "enum": ["2.0", "3.0"]
    },
    "overall_status": {
      "type": "string",
      "enum": ["pass", "fail"]
    },
    "rubric_scores": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "all_artifacts_present",
        "constraint_coverage",
        "traceability",
        "adr_presence",
        "source_integrity",
        "kb_evidence_grounding",
        "repo_grounding",
        "flaw_detection",
        "adr_soundness",
        "best_practice_grounding",
        "refinement_readiness"
      ],
      "properties": {
        "all_artifacts_present": { "type": "integer", "minimum": 0, "maximum": 2 },
        "constraint_coverage": { "type": "integer", "minimum": 0, "maximum": 2 },
        "traceability": { "type": "integer", "minimum": 0, "maximum": 2 },
        "adr_presence": { "type": "integer", "minimum": 0, "maximum": 2 },
        "source_integrity": { "type": "integer", "minimum": 0, "maximum": 2 },
        "kb_evidence_grounding": { "type": "integer", "minimum": 0, "maximum": 2 },
        "repo_grounding": { "type": "boolean" },
        "flaw_detection": { "type": "boolean" },
        "adr_soundness": { "type": "boolean" },
        "best_practice_grounding": { "type": "boolean" },
        "refinement_readiness": { "type": "boolean" }
      }
    },
    "judgment_reasons": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "repo_grounding",
        "flaw_detection",
        "adr_soundness",
        "best_practice_grounding",
        "refinement_readiness"
      ],
      "properties": {
        "repo_grounding": { "type": "string" },
        "flaw_detection": { "type": "string" },
        "adr_soundness": { "type": "string" },
        "best_practice_grounding": { "type": "string" },
        "refinement_readiness": { "type": "string" }
      }
    },
    "issues": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "severity",
          "category",
          "finding",
          "evidence",
          "suggested_fix",
          "requires_refinement",
          "non_refinable"
        ],
        "properties": {
          "id": { "type": "string" },
          "severity": { "type": "string", "enum": ["low", "medium", "high"] },
          "category": {
            "type": "string",
            "enum": [
              "completeness",
              "constraint",
              "grounding",
              "traceability",
              "adr",
              "repo_alignment",
              "evidence",
              "safety"
            ]
          },
          "finding": { "type": "string" },
          "evidence": { "type": "string" },
          "suggested_fix": { "type": "string" },
          "requires_refinement": { "type": "boolean" },
          "non_refinable": {
            "type": "boolean",
            "description": "True only when another Architect pass cannot possibly close this finding within the same run (e.g. a total KB evidence gap - research runs once, before the refine loop). Read by refine_gate.py to stop honestly instead of burning iterations."
          }
        }
      }
    },
    "requires_refinement": {
      "type": "boolean",
      "description": "True unless every code item is full, every VERDICT-BEARING judgment passes, and no high-severity issue exists. Criteria listed in not_applicable, and refinement_readiness, are excluded from that test."
    },
    "refinement_instruction": {
      "type": "string",
      "description": "Code-assembled instruction carrying the finding, evidence and fix of every issue, ranked by severity; empty on pass."
    },
    "not_applicable": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Criteria asked and recorded but carrying no verdict weight, because no evidence existed for them to judge. Must render as n/a, never as a pass."
    }
  }
}
