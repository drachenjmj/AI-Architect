"""pipeline/ — the multi-agent skeleton (orchestrator + agents + shared state).

This package is the *spine* of the AI Solution Architect. It is intentionally
separate from the single-agent prototype in the repo root (architect.py / app.py),
which is kept only as a reference for the system prompt and RAG wiring.
"""
